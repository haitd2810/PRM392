@page "/Admin/Menu"
@model WebAdmin.Pages.Admin.MenuModel
@{
}
<link href="https://unicons.iconscout.com/release/v3.0.6/css/line.css" rel="stylesheet">
<h1 class="h3 mb-3"><strong>Menu</strong> List</h1>
<form method="post">
    <button type="submit" class="btn btn-success">Add</button>
</form>
<div class="row">
    @if (Model.menus != null)
    {
        <table class="table mb-0 table-center">
            <thead>
                <tr>
                    <th class="border-bottom p-3" style="min-width: 50px;">#</th>
                    <th class="border-bottom p-3" style="min-width: 180px;">Nameame</th>
                    <th class="border-bottom p-3">Detail</th>
                    <th class="border-bottom p-3" style="min-width: 150px;">Price</th>
                    <th class="border-bottom p-3" style="min-width: 150px;">Img</th>
                    <th class="border-bottom p-3" style="min-width: 150px;">Quantity</th>
                    <th class="border-bottom p-3" style="min-width: 150px;">Category</th>
                    <th class="border-bottom p-3" style="min-width: 150px;"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var t in Model.menus)
                {
                    <tr>
                        <th class="p-3">@t.Id</th>
                        <td class="p-3">@t.Name</td>
                        <td class="p-3">@t.Detail</td>
                        <td class="p-3">@t.Price</td>
                        <td class="p-3">@t.Img</td>
                        <td class="p-3">@t.Quantity</td>
                        <td class="p-3">@t.Cate.Name</td>
                        <td class="text-end p-3">
                            <a href="/Admin/Menu?Handler=Delete&id=@t.Id" class="btn btn-icon btn-pills btn-soft-danger"><i class="uil uil-times-circle"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }

</div>
